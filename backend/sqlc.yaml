version: "2"
sql:
  - engine: "postgresql"
    queries: "db/query/*.sql"
    schema: "db/migrations/*.sql"
    gen:
      go:
        package: "database"
        out: "database"
        sql_package: "pgx/v5"
